{%- comment -%}
  Konsumentkollen – App Embed Block (Cart Drawer)
  Laddar JS via schema + skickar in asset-URL (logga) och Scrive-URL via data-attribut.
{%- endcomment -%}

<div
  id="kk-embed-root"
  data-logo-url="{{ 'VIPO_LOGGA.svg' | asset_url }}"
  data-scrive-url="{{ settings.scrive_url }}"
></div>

{% schema %}
{
  "name": "Konsumentkollen",
  "target": "body",
  "javascript": "konsumentkollen-cart.js",
  "settings": [
    {
      "type": "url",
      "id": "scrive_url",
      "label": "Scrive redirect URL"
    }
  ]
}
{% endschema %}

